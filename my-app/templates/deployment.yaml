apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.app.appname }}
spec:
  selector:
    matchLabels:
      app: {{ .Values.app.selectorname }}
  replicas: {{ .Values.app.replicas }}
  template:
    metadata:
      labels:
        app: {{ .Values.app.selectorname }}
    spec:
      containers:
        - name: {{ .Values.app.container.containername }}
          image: {{ .Values.app.container.imagename }}
          ports:
            - containerPort: {{ .Values.app.container.port }}
          env:
            - name: RUNNING_IN_CONTAINER
              value: "true"
